__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
.pytest_cache/
.coverage
.tox/
*.log
.DS_Store
.idea/
.vscode/
uv.lock

# tests/fixtures/ — per-session live-MCP working copies + any RDLs
# that aren't ours to commit. Tracked fixtures stay tracked
# (pbi_*.rdl etc.); anything else in this dir is ignored by default.
tests/fixtures/*.rdl*
